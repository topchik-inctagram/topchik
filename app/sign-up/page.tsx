'use client'
import { PageContainer } from '@/shared/components'
import { SignUp } from '@/features'

const Page = () => {
  return (
    <PageContainer>
      <SignUp onSubmit={() => {}} />
    </PageContainer>
  )
}

export default Page
